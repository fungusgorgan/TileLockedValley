<Project>
  <PropertyGroup>
    <!-- Instructs SMAPI to bundle the compiled mod zip here -->
    <ModZipPath>$(MSBuildThisFileDirectory)/_releases</ModZipPath>
    <EnableModDeploy>false</EnableModDeploy>
  </PropertyGroup>
</Project>